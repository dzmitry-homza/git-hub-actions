name: 'Stale PRs check'
permissions:
pull-requests: write
on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  stale-prs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v8
        with:
        days-before-close: -1
        days-before-stale: 0
        only-labels: 'dispatch-team'
